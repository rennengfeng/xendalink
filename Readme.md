### 上传文件至部署服务器比如 /home
### 使用方法
```bash
cd /home
chmod +x openclaw.sh
sed -i -e 's/\r$//' openclaw.sh
./openclaw.sh
```
跟着指引完成操作。注意如果提示`run error: HTTP 403: Your request was blocked`需联系客服，并告知服务器IP。
